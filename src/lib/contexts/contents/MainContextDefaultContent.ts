import { MainContextContentInterface } from "../interfaces/MainContext";

const MainContextDefaultContent: MainContextContentInterface = {
  user: undefined,
  ready: false,
};

export default MainContextDefaultContent;
