import { ViewContextContentInterface } from "../interfaces/ViewContext";

const ViewContextDefaultContent: ViewContextContentInterface = {
  ready: true,
  books: "grid",
  theme: "light",
  loading: false,
};

export default ViewContextDefaultContent;
